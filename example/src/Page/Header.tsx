import { Box } from '@mui/material';
import React from 'react';

export const Header = () => {
  return (
    <Box
      sx={{
        minHeight: '200px',
        backgroundColor: 'lightgreen',
        border: '3px solid black',
      }}
    >
      Header
    </Box>
  );
};
